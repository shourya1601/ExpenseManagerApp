<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.shourya.expensemanager">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ExpenseManager">
        <activity android:name=".about"
            android:theme="@style/Theme.AppCompat.Dialog"></activity>
        <activity android:name=".help" />
        <activity android:name=".custom_date_display" />
        <activity android:name=".budget" />
        <activity android:name=".splash_screen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".update_category" />
        <activity android:name=".add_category" />
        <activity android:name=".update_listitem" />
        <activity android:name=".piechart" />
        <activity android:name=".selectedCategory" />
        <activity android:name=".passbook" />
        <activity android:name=".customDateRange" />
        <activity android:name=".manage_categories" />
        <activity android:name=".income" />
        <activity
            android:name=".MainActivity"
            android:theme="@style/Theme.Design" />
    </application>

</manifest>